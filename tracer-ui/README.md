## tracer-ui

```bash
make build
export MANUALRUN=1
make dev
```

In one window:

```bash
make api.cli
yarn serve
```

In another window:

```bash
make frontend.cli
yarn develop
```

Then open http://localhost